import { FormInputErrorDirective } from './form-input-error.directive';

describe('FormInputErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputErrorDirective();
    expect(directive).toBeTruthy();
  });
});
